apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-volv-pvc
  namespace: uptime
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "rock-cephfs"
  resources:
    requests:
      storage: 2Gi