apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
  annotations: 
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nasa.markaplay.net`) && (PathPrefix(`/traefik`)
      kind: Rule
      middlewares:
        - name: strip-traefik-prefix
          namespace: traefik
      services:
        - name: api@internal
          kind: TraefikService
